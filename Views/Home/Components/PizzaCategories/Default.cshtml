@model IEnumerable<Category>

    <h3 class="pt-4 text-warning">Pizza Categories</h3>
    <div class="pt-4">
        <div>
            <a  class="btn btn-block btn-@(ViewBag.SelectedCatName=="" ? "primary": "outline-dark")" asp-route-pizzaCatName="" asp-action="AllPizzas">
                All Pizzas
            </a>
        </div>
        @foreach (Category c in Model)
        {

            <div class="mt-2">
                <a class="btn btn-block btn-@(ViewBag.SelectedCatName==c.Name ? "primary": "outline-dark")" 
                   asp-route-pizzaCatName="@c.Name" asp-action="AllPizzas">@c.Name</a>
            </div>

        }
    </div>